# 🚀 Инструкция по деплою на Vercel и подключению домена swier.ru

## Шаг 1: Подготовка проекта

### 1.1 Загрузите изменения в GitHub

```bash
git add .
git commit -m "Add Vercel configuration"
git push
```

## Шаг 2: Деплой на Vercel

### 2.1 Создайте аккаунт на Vercel

1. Перейдите на [vercel.com](https://vercel.com)
2. Нажмите **Sign Up**
3. Войдите через GitHub аккаунт (gogaboy1)

### 2.2 Импортируйте проект

1. На главной странице Vercel нажмите **Add New** → **Project**
2. Найдите репозиторий **swier** в списке
3. Нажмите **Import**

### 2.3 Настройте переменные окружения

В разделе **Environment Variables** добавьте:

| Name | Value |
|------|-------|
| `JWT_SECRET` | Случайная строка (например: `a8f5f167f44f4964e6c998dee827110c`) |
| `ADMIN_PASSWORD` | Ваш пароль администратора |
| `NEXT_PUBLIC_APP_URL` | `https://swier.ru` |
| `DATABASE_URL` | `file:./prisma/dev.db` |

**Опционально (для платежей):**
- `ROBOKASSA_MERCHANT_LOGIN`
- `ROBOKASSA_PASSWORD_1`
- `ROBOKASSA_PASSWORD_2`
- `ROBOKASSA_TEST_MODE` = `true`

### 2.4 Запустите деплой

1. Нажмите **Deploy**
2. Дождитесь завершения сборки (2-3 минуты)
3. Получите временный URL вида `swier.vercel.app`

## Шаг 3: Подключение домена swier.ru

### 3.1 Добавьте домен в Vercel

1. Откройте ваш проект в Vercel
2. Перейдите в **Settings** → **Domains**
3. Введите `swier.ru` и нажмите **Add**
4. Также добавьте `www.swier.ru`

### 3.2 Настройте DNS записи у регистратора домена

Vercel покажет вам DNS записи, которые нужно добавить. Обычно это:

#### Для основного домена (swier.ru):

**Тип A запись:**
```
Type: A
Name: @
Value: 76.76.21.21
```

**Или CNAME (если A не поддерживается):**
```
Type: CNAME
Name: @
Value: cname.vercel-dns.com
```

#### Для www поддомена:

```
Type: CNAME
Name: www
Value: cname.vercel-dns.com
```

### 3.3 Где настроить DNS

Зайдите в панель управления вашего регистратора домена (где вы купили swier.ru):

- **Если это reg.ru**: Личный кабинет → Домены → swier.ru → Управление DNS
- **Если это nic.ru**: Личный кабинет → Домены → DNS-серверы
- **Если это timeweb/beget**: Панель управления → DNS

### 3.4 Проверка

1. DNS изменения применяются от 5 минут до 48 часов (обычно 15-30 минут)
2. Vercel автоматически выпустит SSL сертификат
3. Проверьте работу сайта: https://swier.ru

## Шаг 4: Настройка базы данных для production

⚠️ **Важно:** SQLite (dev.db) не подходит для production на Vercel.

### Рекомендуемые варианты:

#### Вариант 1: Vercel Postgres (рекомендуется)

1. В проекте Vercel: **Storage** → **Create Database** → **Postgres**
2. Скопируйте `DATABASE_URL` из Vercel
3. Обновите `prisma/schema.prisma`:

```prisma
datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
```

4. Запустите миграцию:
```bash
npx prisma migrate deploy
npx prisma db seed
```

#### Вариант 2: Supabase (бесплатно)

1. Создайте проект на [supabase.com](https://supabase.com)
2. Получите PostgreSQL connection string
3. Добавьте в Environment Variables Vercel

#### Вариант 3: PlanetScale (MySQL)

1. Создайте базу на [planetscale.com](https://planetscale.com)
2. Получите connection string
3. Измените provider в schema.prisma на `mysql`

## Шаг 5: Автоматический деплой

После настройки каждый push в GitHub будет автоматически деплоиться на Vercel:

```bash
git add .
git commit -m "Ваши изменения"
git push
```

Vercel автоматически:
- Соберет проект
- Запустит тесты
- Задеплоит на production (swier.ru)
- Создаст preview для веток

## Полезные команды

```bash
# Локальная разработка
npm run dev

# Сборка проекта
npm run build

# Запуск production версии локально
npm start

# Обновление базы данных
npx prisma migrate dev
npx prisma db seed
```

## Проверка работы

После деплоя проверьте:

- ✅ Главная страница: https://swier.ru
- ✅ Авторизация: https://swier.ru/auth/signin
- ✅ Регистрация: https://swier.ru/auth/signup
- ✅ Свайп стартапов
- ✅ Комментарии
- ✅ Админ панель: https://swier.ru/admin

## Поддержка

- [Документация Vercel](https://vercel.com/docs)
- [Документация Next.js](https://nextjs.org/docs)
- [Prisma Docs](https://www.prisma.io/docs)

---

🎉 **Готово! Ваш сайт доступен по адресу https://swier.ru**
